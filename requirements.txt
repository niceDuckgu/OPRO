# --- Core ----------------------------------------------------------------
torch>=2.4
numpy>=1.24
Pillow>=10.0
PyYAML>=6.0

# --- Tests ---------------------------------------------------------------
pytest>=8.0

# --- FluxFill / ICEdit / ACE++ / UNO integrations ------------------------
# Install only when running Track A / B / D scripts that hit FluxFill.
# diffusers>=0.30
# peft>=0.10
# accelerate>=0.30
# safetensors>=0.4
# transformers>=4.40
# huggingface-hub>=0.24
